# Copy to .signing.env in the repo root (gitignored). Use single quotes if the password contains ! or spaces.
RELEASE_KEY_PASSWORD='your-upload-keystore-password'
# Optional if different from RELEASE_KEY_PASSWORD:
# RELEASE_KEYSTORE_PASSWORD='...'
